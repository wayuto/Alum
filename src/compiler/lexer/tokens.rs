#[derive(Debug, Clone, PartialEq)]
pub enum FstringSeg {
    Lit(String),
    Expr(String),
}

#[derive(Debug, Clone, PartialEq)]
pub enum Token {
    INT(isize),
    FLOAT(f64),
    BOOL(bool),
    STRING(String),
    CHAR(u8),
    FSTRING(Vec<FstringSeg>),
    NIL,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    PERCENT,
    DOT,
    DOTDOT,
    CEQ,
    NE,
    LT,
    LE,
    GT,
    GE,
    AND,
    OR,
    LAND,
    LOR,
    XOR,
    NOT,
    SHL,
    SHR,
    BNOT,
    STAREQ,
    SLASHEQ,
    PERCENTEQ,
    ANDEQ,
    OREQ,
    XOREQ,
    SHLEQ,
    SHREQ,
    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    LBRACKET,
    RBRACKET,
    EQ,
    PLUSEQ,
    MINUSEQ,
    PLUSPLUS,
    MINUSMINUS,
    COLON,
    COLONCOLON,
    COMMA,
    SEMICOLON,
    VAR,
    CST,
    FUN,
    FOR,
    IN,
    EXTERN,
    IMPORT,
    USING,
    AS,
    RET,
    IF,
    ELSE,
    WHILE,
    BREAK,
    CONTINUE,
    TYPEDEF,
    MATCH,
    STRUCT,
    UNION,
    ENUM,
    TYPE(String),
    IDENT(String),
    AT,
    DOLLAR,
    LAMBDA,
    EOF,
}
