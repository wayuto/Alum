[package]
name = "alc"
version = "0.9.7"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
ordered-float = "5.0.0"

[profile.release]
opt-level = 3
lto = "fat"      
codegen-units = 1
panic = "abort"      
strip = "symbols"