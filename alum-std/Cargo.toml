[package]
name = "alum-std"
version = "0.9.6"
edition = "2024"

[lib]
name = "alum_std"
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.release]
panic = "abort"
opt-level = 3

[profile.dev]
panic = "abort"
