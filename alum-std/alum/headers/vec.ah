$ifndef ALUM_VEC
$define ALUM_VEC 1

struct Vec<T> {
    data: T[],
    len: int,
    capacity: int,
    at: T(*Vec<T>, int),
    push: void(*Vec<T>, T),
    pop: T(*Vec<T>),
    clear: void(*Vec<T>),
}

extern vec_new(): Vec<int>

$endif
