$import "gosio"
$import "convert"

$define PI 3.14

fun f(n: flt): flt {
    return n * n * PI
}

pub fun main(): int {
    let n: flt = atof(
        input("Enter a number: ")
    )
    println(
        ftoa(
            f(n)
        )
    )
    return 0
}
